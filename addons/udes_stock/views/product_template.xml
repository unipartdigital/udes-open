<?xml version="1.0"?>
<odoo>
    <record id="view_product_template_form_view_udes_stock" model="ir.ui.view">
        <field name="name">product.template.common.form.udes_stock</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_form_view"/>
        <field name="arch" type="xml">
            <xpath expr="//page[@name='inventory']" position="inside">
                <group string="Inventory" name="inventory_udes">
                    <field name="u_height_category_id" />
                    <field name="u_speed_category_id" />
                    <field name="u_height" />
                    <field name="u_length" />
                    <field name="u_width" />
                    <field name="u_volume" />
                    <field name="u_pack_qty" />
                    <field name="u_carton_qty" />
                    <field name="u_pallet_qty" />
                </group>
            </xpath>
            <xpath expr="//div[@name='options']" position="inside">
                <field name="u_multiple_barcodes" invisible="1"/>
            </xpath>
        </field>
    </record>

    <record id="product_template_only_form_view_udes_stock" model="ir.ui.view">
        <field name="name">product.template.product.form.udes_stock</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_only_form_view"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='barcode']" position="attributes">
                <attribute name="attrs">{'invisible': [('u_multiple_barcodes', '=', True)]}</attribute>
            </xpath>
            <xpath expr="//field[@name='description']/parent::group" position="before">
                <group string="Barcodes" attrs="{'invisible':['|', ('u_multiple_barcodes','=',False), ('product_variant_count', '>', 1)]}">
                    <field name="u_barcode_ids" nolabel="1">
                        <tree string="Barcodes" editable="bottom">
                            <field name="name" required="1"/>
                        </tree>
                    </field>
                </group>
            </xpath>
        </field>
    </record>

     <!-- Template search view -->
    <record id="view_product_template_search_view_udes_stock" model="ir.ui.view">
        <field name="name">product.template.search.udes_stock</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_search_view"/>
        <field name="arch" type="xml">
            <!-- Add a new filter for Active products, as we now include inactive by default -->
            <filter name="inactive" position="after">
                <filter string="Active" name="active" domain="[('active','=',True)]"/>
            </filter>
        </field>
    </record>

    <!-- Extend template actions to include inactive products in context
    which results in archived templates still showing a barcode from the related field,
    as m2o relations are hidden via the ORM when the linked recordset is inactive - even if
    the context is set on the barcode field (related seems to ignore context);
    >>> self.env["product.template"].browse(xxx).product_variant_ids
    product.product()
    >>> self.env["product.template"].browse(xxx).with_context(dict(active_test=False)).product_variant_ids
    product.product(xxx,)
    Also include a default filter 'Active is false' to hide them by default -->
    <record id="product.product_template_action" model="ir.actions.act_window">
        <field name="context">{"search_default_filter_to_sell":1, "active_test": False, "search_default_active": 1}</field>
    </record>
    <record id="stock.product_template_action_product" model="ir.actions.act_window">
        <field name="context">{"search_default_consumable": 1, 'default_type': 'product', "active_test": False, "search_default_active": 1}</field>
    </record>

    <record id="action_upload_image_list" model="ir.actions.act_window">
        <field name="name">Upload Product Image</field>
        <field name="res_model">stock.upload.product.image.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
        <field name="context">{'default_product_ids': active_ids}</field>
        <field name="binding_model_id" ref="product.model_product_template"/>
        <field name="binding_view_types">list</field>
    </record>


    <record id="view_product_form_inherit_upload_photo" model="ir.ui.view">
        <field name="name">product.template.form.inherit.upload.photo</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_only_form_view"/>
        <field name="arch" type="xml">
            <header position="inside">
                <button name="%(action_upload_product_image_wizard)d"
                        string="Upload Image"
                        type="action"
                        class="oe_highlight"/>
            </header>
        </field>
    </record>
</odoo>
