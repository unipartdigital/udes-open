name: test-addons

on:
  pull_request:
  push:

jobs:
  test-addons:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install Python 3
        uses: actions/setup-python@v2
        with:
          python-version: 3.6
